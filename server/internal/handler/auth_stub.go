package handler

// StubUserID is the hardcoded user identity used while OIDC auth is not yet implemented.
// Replace with real session validation once IDP credentials are available (see docs/deferred.md).
const StubUserID = "dev"
const stubUserID = StubUserID

const stubUserName = "Dev"
